<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingStart="32dp"
    android:paddingEnd="32dp"
    android:paddingTop="8dp"
    android:paddingBottom="8dp">

    <TextView
        android:id="@+id/participant_nick_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:textSize="18sp" />

    <TextView
        android:id="@+id/role_flag"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:layout_toEndOf="@id/participant_nick_name"
        android:layout_centerVertical="true"
        android:background="@drawable/oval_button_background"
        android:text="主播"
        android:textSize="12sp"
        android:textColor="@android:color/white" />

    <ImageButton
        android:id="@+id/mute_audio_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:background="@android:color/transparent"
        android:src="@drawable/ic_voice"
        android:visibility="invisible" />

    <ImageButton
        android:id="@+id/mute_video_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginEnd="32dp"
        android:layout_toStartOf="@id/mute_audio_btn"
        android:background="@android:color/transparent"
        android:src="@drawable/ic_video"
        android:visibility="gone" />

</RelativeLayout>
